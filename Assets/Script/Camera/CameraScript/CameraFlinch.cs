using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CameraFlinch : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // UpdateNode is called once per frame
    void Update()
    {
        
    }
}
